services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: giacobello_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
    ports:
      - "5433:5432"
    volumes:
      - restaurante_data:/var/lib/postgresql/data

volumes:
  restaurante_data:
